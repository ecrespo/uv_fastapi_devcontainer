services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: app
    ports:
      - "8000:8000"
    networks:
      - fastapi_api
    restart: always

networks:
  fastapi_api:
    driver: bridge